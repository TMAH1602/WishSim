# 2026-07-20 — v0.5.0 feature release

## Intent

- Publish the game shell, persistent teams, Character Management, weapon equipment, three-star raster weapon art, Corvin complexion correction, and final ten-pull skip behavior documented in `2026-07-20-game-shell-teams-equipment.md`.
- Preserve Ghostty as the primary supported terminal while retaining Kitty protocol and ANSI fallback support.

## Version and scope

- Release version: `0.5.0` / tag `v0.5.0`.
- Source baseline before release work: `a84f364`, the publication record following v0.4.0.
- Release changes begin in the working tree and must not be described as committed or published until each corresponding publication step completes.
- Version metadata changes: `Cargo.toml`, `Cargo.lock`, and `CHANGELOG.md`.

## Publication plan

1. Run locked formatting, checks, tests, strict Clippy, release build, and diff validation.
2. Commit the complete feature release and push `main`.
3. Create and push annotated tag `v0.5.0`.
4. Wait for the GitHub release workflow and verify all four platform archives.
5. Update both macOS URLs and SHA-256 values in `TMAH1602/homebrew-wishsim`.
6. Run Ruby syntax, Homebrew style/audit/test checks, push the tap, upgrade the installed formula, and verify `wishsim --version`.

## Validation and final results

- Pre-publication validation passed: `cargo fmt --all -- --check`, `cargo check --locked`, `cargo test --locked` (26 tests), `cargo clippy --locked --all-targets -- -D warnings`, `cargo build --release --locked`, and `git diff --check`.
- Publication identifiers and Homebrew verification remain pending until those steps complete.

## Safe backtracking

- Do not rewrite a published tag. If v0.5.0 is published and needs correction, create a later patch release.
- Before publication, reverse only the version/release-note hunks in `Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, and this log if packaging is cancelled. Do not remove the underlying feature work.
