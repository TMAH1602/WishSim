# 2026-07-23 — v0.6.1 patch release

## Intent

- Publish the corrected Yeoungin alpha matte and cursor-following viewport behavior for both character selection lists.
- Preserve the complete v0.6.0 feature set and Ghostty-primary rendering contract.

## Version and scope

- Release version: `0.6.1` / tag `v0.6.1`.
- Published baseline: v0.6.0 documentation tip `96dbb5d`, release commit `2fe8bd1`, and tag `v0.6.0`.
- Patch details and rollback guidance are documented in `2026-07-23-yeoungin-alpha-roster-scroll-fix.md`.
- Version metadata changes: `Cargo.toml`, `Cargo.lock`, and `CHANGELOG.md`.

## Publication plan

1. Run locked formatting, checks, tests, strict Clippy, release build, explicit Yeoungin alpha assertions, and diff validation.
2. Commit the patch and push `main`.
3. Create and push annotated tag `v0.6.1`.
4. Wait for the GitHub release workflow and verify all four platform archives.
5. Update both macOS URLs and SHA-256 values in `TMAH1602/homebrew-wishsim`.
6. Run Ruby syntax, Homebrew style/audit/test checks, push the tap, upgrade the installed formula, and verify `wishsim --version`.

## Validation and final results

- Pre-publication validation passed: `cargo fmt --all -- --check`, `cargo check --locked`, `cargo test --locked` (38 passed, 0 failed), `cargo clippy --locked --all-targets -- -D warnings`, `cargo build --release --locked`, `git diff --check`, and explicit Yeoungin 1024×1536 RGBA/corner/face-pixel assertions.
- Source commit, tag, workflows, checksums, tap commit, and installed version will be recorded only after confirmed publication.

## Safe backtracking

- Do not rewrite the published v0.6.0 or v0.6.1 tags. If a published correction needs another fix, create a later patch release.
- Before publication, reverse only the version/release-note hunks and this release log if packaging is cancelled. Do not remove the underlying fixes.
