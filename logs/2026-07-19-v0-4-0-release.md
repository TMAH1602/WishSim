# 2026-07-19 — v0.4.0 feature release

## Intent

- Publish the accumulated banner, roster, weapon-art, Standard Archive, weapon-selector, and Character Archive work documented in the 2026-07-18 and 2026-07-19 feature logs.
- Designate Ghostty as WishSim's primary supported terminal emulator. Kitty remains supported through the shared graphics protocol and ANSI remains the portable fallback.

## Version and scope

- Release version: `0.4.0` / tag `v0.4.0`.
- Source baseline before release work: `0c6b30f`, the publication record following v0.3.1.
- Release changes are initially in the working tree and must not be described as committed or published until the corresponding steps complete.
- Version metadata changes: `Cargo.toml`, `Cargo.lock`, and `CHANGELOG.md`.
- User-facing terminal-support notes updated in `README.md` without removing Kitty or ANSI behavior.

## Publication plan

1. Run locked formatting, tests, strict Clippy, release build, and diff validation.
2. Commit the complete feature release and push `main`.
3. Create and push annotated tag `v0.4.0`.
4. Wait for the GitHub release workflow and verify all four platform assets.
5. Update both macOS URLs and SHA-256 values in `TMAH1602/homebrew-wishsim`.
6. Run Ruby syntax, Homebrew style/audit/test checks, push the tap, upgrade the installed formula, and verify `wishsim --version`.

## Validation and final results

- Pre-publication validation passed: `cargo fmt --all -- --check`, `cargo check --locked`, `cargo test --locked` (21 tests), `cargo clippy --locked --all-targets -- -D warnings`, `cargo build --release --locked`, and `git diff --check`.
- Source release commit: `dacf0d6` (`Release WishSim v0.4.0`).
- Annotated tag `v0.4.0` and `main` were pushed to `TMAH1602/WishSim`.
- GitHub release: `https://github.com/TMAH1602/WishSim/releases/tag/v0.4.0`.
- Release workflow run `29687323445` completed successfully for Linux x86_64, macOS ARM64, macOS x86_64, and Windows x86_64; all four archives are attached.
- macOS ARM64 SHA-256: `ccd46f9ef73e86fddb91988cad8a1f4b91ab2314b28467e0eda1b882911ec318`.
- macOS x86_64 SHA-256: `60c8aa3222da9d478dbb82f899a8cae65da860ddb362a128e3fce8af33bab671`.
- Homebrew tap commit: `b5b3d89` (`Update WishSim to v0.4.0`).
- Formula Ruby syntax and `brew style` passed. The published name-based strict online audit completed without WishSim findings.
- Homebrew upgraded the installed package from 0.3.1 to 0.4.0, checksum-verified the archive, passed `brew test TMAH1602/wishsim/wishsim`, and returned `wishsim 0.4.0` from `wishsim --version`.
- Homebrew emitted unrelated warnings about a font-cask metadata migration and an untrusted third-party tap; neither affected WishSim publication or validation.

## Safe backtracking

- Do not rewrite a published tag. If v0.4.0 is published and needs correction, create a later patch release.
- Before publication, reverse only the version/release-note hunks in `Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, `README.md`, and this log if release packaging is cancelled. Do not remove the underlying feature work.
