# 2026-07-23 — v0.6.0 feature release

## Intent

- Publish the Battle Test foundation, Yeoungin and Rimebound Benediction, the early three-tier ability system, smarter enemy tactics and poison, level-50 weapon projection, and four-star release companions Seo-yeon and Ji-ho.
- Preserve Ghostty as the primary supported terminal while retaining Kitty protocol and portable ANSI support.

## Version and scope

- Release version: `0.6.0` / tag `v0.6.0`.
- Source baseline before this accumulated feature work: published v0.5.0 documentation tip `b26672b`.
- Release changes begin in the working tree and must not be described as committed or published until each corresponding publication step completes.
- Version metadata changes: `Cargo.toml`, `Cargo.lock`, and `CHANGELOG.md`.

## Publication plan

1. Run locked formatting, checks, tests, strict Clippy, release build, asset validation, and diff validation.
2. Commit the complete feature release and push `main`.
3. Create and push annotated tag `v0.6.0`.
4. Wait for the GitHub release workflow and verify all four platform archives.
5. Update both macOS URLs and SHA-256 values in `TMAH1602/homebrew-wishsim`.
6. Run Ruby syntax, Homebrew style/audit/test checks, push the tap, upgrade the installed formula, and verify `wishsim --version`.

## Validation and final results

- Pre-publication validation passed: `cargo fmt --all -- --check`, `cargo check --locked`, `cargo test --locked` (37 passed, 0 failed), `cargo clippy --locked --all-targets -- -D warnings`, `cargo build --release --locked`, `git diff --check`, and explicit 1024×1536 alpha inspection for Seo-yeon and Ji-ho.
- Source release commit, tag, workflow, archive checksums, tap commit, and installed version will be recorded only after confirmed publication.

## Safe backtracking

- Do not rewrite a published tag. If v0.6.0 is published and needs correction, create a later patch release.
- Before publication, reverse only version/release-note hunks in `Cargo.toml`, `Cargo.lock`, `CHANGELOG.md`, and this log if packaging is cancelled. Do not remove the underlying feature work.
